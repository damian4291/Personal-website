<h3>Damian Zawadziński</h3>
<ul class="contact__links">
    {{#each links}}
        <li>
            <span class="contact__icon icon__image icon--equal" data-icon="{{ icon }}" aria-hidden="true"></span>
            <a href="{{ link.href }}" {{#if link._blank}}target="_blank"{{/if}}>{{ title }}</a>
        </li>
    {{/each}}
</ul>
